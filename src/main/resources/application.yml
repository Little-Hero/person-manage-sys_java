spring:
  datasource:
    username: root
    password: root
    url: jdbc:mysql://localhost:3306/manger?serverTimezone=UTC&characterEncoding=utf-8
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database: mysql